#Totally useless file because I didn't feel like renaming Ctype.py
from ASearch.Ctype import getDist


def ASearch(perm):
    return getDist(perm)
def getDistRange(perm, 0, n+5)

def flip(state, move):
    state[0:move+1] = state[0:move+1][::-1]
    return state